import java.io.File;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Scanner;


public class Prog2 {
	/*public static void main( String[] args ) throws IOException {
		RandomizedSelect sel = new RandomizedSelect();
		sel.loadStore("data/WhataburgerData.csv");
		System.out.println(sel);
		
		Store qu= new Store();
		qu.computeDistance(0, 0)*/
		
		/*Scanner scan = new Scanner(new File ("data/Queries.csv"));
		scan.nextLine(); //skips the header field
		while(scan.hasNextLine()) {
			String line = scan.nextLine();
			String[] tokens = line.split( "," );
			if( tokens!=null && tokens.length==3) {
				String lat = tokens[0];
				String longt= tokens[1];
				String num= tokens[2];

		}
		}
		scan.close();*/
		
		/*int[] data = {1, 3, 6, 2, 5, 11, 8, 7, 0, -1};
		int rank = 3;
		RandomizedSelect selection = new RandomizedSelect(data, rank);
		System.out.println("The rank = " + rank + " of the array is " + selection.iterativeSelect());*/
	
	
}
